from django.apps import AppConfig


class TimetableConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'django_school_management.timetable'
    verbose_name = 'Timetable & Scheduling'
